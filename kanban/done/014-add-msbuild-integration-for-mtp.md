# Add MSBuild Integration for M.T.P.

## Summary

Create MSBuild `.props` and `.targets` files that configure consuming projects for Microsoft.Testing.Platform integration. This enables automatic `dotnet test` support and IDE Test Explorer discovery.

**Parent Epic**: 010 - Microsoft.Testing.Platform Integration

## Todo List

### Props File
- [x] Create `build/TimeWarp.Jaribu.TestingPlatform.props`
- [x] Set `IsTestProject=true`
- [x] Set `IsTestingPlatformApplication=true`
- [x] Set `OutputType=Exe`
- [x] Set `GenerateProgramFile=false`
- [x] Register `TestingPlatformBuilderHook`

### Package Configuration
- [x] Configure `TfmSpecificPackageFile` for build assets
- [x] Include in both `build/` and `buildTransitive/`
- [ ] Test NuGet package layout

### Validation
- [x] Create test project that references the package
- [x] Verify `dotnet test` works
- [x] Verify `dotnet run` works
- [x] Verify properties are applied correctly

## Notes

### Props File Content

```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- build/TimeWarp.Jaribu.TestingPlatform.props -->
<Project>

  <PropertyGroup>
    <!-- Mark as test project for IDE recognition -->
    <IsTestProject>true</IsTestProject>
    
    <!-- Enable Microsoft.Testing.Platform -->
    <IsTestingPlatformApplication>true</IsTestingPlatformApplication>
    <TestingPlatformDotnetTestSupport>true</TestingPlatformDotnetTestSupport>
    
    <!-- Build as executable (M.T.P. generates Main) -->
    <OutputType>Exe</OutputType>
    <GenerateProgramFile>false</GenerateProgramFile>
    
    <!-- Enable protocol for IDE integration -->
    <UseTestingPlatformProtocol>true</UseTestingPlatformProtocol>
    <IsTestingPlatformEnabled>true</IsTestingPlatformEnabled>
  </PropertyGroup>

  <ItemGroup>
    <!-- 
      Register the Jaribu test framework with M.T.P.
      The GUID should be unique and stable for this extension.
    -->
    <TestingPlatformBuilderHook Include="TimeWarp.Jaribu.TestingPlatform">
      <DisplayName>TimeWarp.Jaribu</DisplayName>
      <TypeFullName>TimeWarp.Jaribu.TestingPlatform.TestingPlatformBuilderHook</TypeFullName>
    </TestingPlatformBuilderHook>
  </ItemGroup>

</Project>
```

### Project File Updates

```xml
<!-- TimeWarp.Jaribu.TestingPlatform.csproj additions -->
<ItemGroup>
  <!-- Include props in both build and buildTransitive for package consumers -->
  <None Include="build/**" Pack="true" PackagePath="build/" />
  <None Include="build/**" Pack="true" PackagePath="buildTransitive/" />
</ItemGroup>
```

### How M.T.P. Uses These Properties

1. **`IsTestingPlatformApplication=true`**: Tells MSBuild to use M.T.P. infrastructure
2. **`GenerateProgramFile=false`**: Prevents SDK from generating `Main()`; M.T.P. generates it
3. **`TestingPlatformBuilderHook`**: M.T.P. looks for this item group to find framework registrations
4. **`TypeFullName`**: Points to the static class with `AddExtensions(ITestApplicationBuilder, string[])` method

### Generated Entry Point

M.T.P. generates something like:

```csharp
// Auto-generated by Microsoft.Testing.Platform.MSBuild
public static class Program
{
    public static async Task<int> Main(string[] args)
    {
        var builder = await TestApplication.CreateBuilderAsync(args);
        
        // Calls our hook
        TimeWarp.Jaribu.TestingPlatform.TestingPlatformBuilderHook
            .AddExtensions(builder, args);
        
        using var app = await builder.BuildAsync();
        return await app.RunAsync();
    }
}
```

### Testing the Integration

Create a test project:

```xml
<!-- Tests/MtpIntegrationTest/MtpIntegrationTest.csproj -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../../Source/TimeWarp.Jaribu.TestingPlatform/TimeWarp.Jaribu.TestingPlatform.csproj" />
  </ItemGroup>
</Project>
```

```csharp
// Tests/MtpIntegrationTest/SampleTests.cs
public class SampleTests
{
    [ModuleInitializer]
    internal static void Register() => RegisterTests<SampleTests>();

    public static async Task PassingTest()
    {
        await Task.CompletedTask;
    }

    public static async Task FailingTest()
    {
        await Task.CompletedTask;
        throw new Exception("Expected failure");
    }
}
```

Verify:
```bash
dotnet test Tests/MtpIntegrationTest/
dotnet test --list-tests
dotnet test --filter "Name~Passing"
```

## Results

**Completed: 2025-12-23**

### What was done:

1. **Updated props file** (`Source/TimeWarp.Jaribu.TestingPlatform/build/TimeWarp.Jaribu.TestingPlatform.props`):
   - Added all required M.T.P. properties: `IsTestProject`, `IsTestingPlatformApplication`, `TestingPlatformDotnetTestSupport`, `OutputType=Exe`, `GenerateProgramFile=false`, `UseTestingPlatformProtocol`, `IsTestingPlatformEnabled`
   - Changed `TestingPlatformBuilderHook` from a simple property to an ItemGroup with proper `Include`, `DisplayName`, and `TypeFullName` attributes

2. **Created validation test project** (`Tests/TimeWarp.Jaribu.MtpValidation/`):
   - Project references TimeWarp.Jaribu.TestingPlatform
   - Explicitly imports the props file (needed for ProjectReference; NuGet packages auto-import from build/)
   - Contains 2 sample tests using the standard Jaribu pattern

3. **Updated Tests/Directory.Build.props**:
   - Added CA2255 to NoWarn (ModuleInitializer attribute warning is expected in test projects)

### Validation Results:

```
$ dotnet test Tests/TimeWarp.Jaribu.MtpValidation/
  Run tests: '...TimeWarp.Jaribu.MtpValidation.dll' [net10.0|x64]
  Passed! - Failed: 0, Passed: 2, Skipped: 0, Total: 2, Duration: 24ms

$ dotnet run --project Tests/TimeWarp.Jaribu.MtpValidation/
  .NET Testing Platform v1.5.3+2a34431d75 (UTC 01/27/2025) [linux-x64 - net10.0]
  Test run summary: Passed!
    total: 2, succeeded: 2, failed: 0, skipped: 0, duration: 28ms
```

### Note for NuGet consumers:

When consuming via NuGet package, the props file is automatically imported from `build/` or `buildTransitive/`. For local development with ProjectReference, projects must explicitly import the props file:

```xml
<Import Project="../../Source/TimeWarp.Jaribu.TestingPlatform/build/TimeWarp.Jaribu.TestingPlatform.props" />
```
